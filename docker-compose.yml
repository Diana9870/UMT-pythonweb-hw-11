version: "3.9"

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: contacts
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432"

  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db